---
name: a-stock-daily-review
description: 生成A股每日短线复盘报告（盘后）。适用场景：用户要求复盘今天/指定交易日的A股市场、统计涨停/炸板/跌停、分析昨日涨停溢价与晋级率、整理连板梯队、梳理板块结构与新老题材、看成交额前排与龙虎榜机构/游资动向、标记亏钱效应、筛选次日风向标个股，并输出可直接打开的 HTML 报告。使用本技能时运行 scripts/daily_review.py 抓取公开行情接口并生成报告。
---

# A股每日短线复盘

## 快速开始

运行脚本（默认复盘今天，需收盘后执行）：

```bash
python3 ~/.codex/skills/a-stock-daily-review/scripts/daily_review.py
```

指定交易日或输出目录：

```bash
python3 ~/.codex/skills/a-stock-daily-review/scripts/daily_review.py --date 20260806
python3 ~/.codex/skills/a-stock-daily-review/scripts/daily_review.py --date 20260806 --out /path/to/outputs
python3 ~/.codex/skills/a-stock-daily-review/scripts/daily_review.py --date 20260806 --name 复盘_自动版_v2
```

参数：
- `--date YYYYMMDD`：交易日，默认今天。
- `--out DIR`：HTML 输出目录，默认 `outputs/`（不存在则输出到当前目录）。
- `--name NAME`：HTML 文件名（不含扩展名），默认 `A股短线复盘_YYYY-MM-DD`；用于生成对比版本时指定不同文件名。
- `--no-theme`：跳过同花顺热点题材抓取（主题标签为空时不影响报告）。

注意：生成的报告文件名默认带上日期（如 `A股短线复盘_2026-08-06.html`），复盘输出时保留日期前缀，便于按天对比验证；用 `--name` 自定义时也建议带上日期。

日期与实时数据规则：
- 指数自动按 `--date` 取腾讯日 K 历史收盘（非当前交易日时也准确）；目标日=当前快照日时用实时快照。
- 涨跌家数、成交额榜、全市场大跌榜只有“当前交易日实时快照”，跨交易日运行（如次日盘中复盘昨日）时自动缺省并在报告中标注，不混入错误日期数据。
- 当天 15:00 后运行会同时在输出目录写入 `review_snapshot_YYYYMMDD.json` 收盘快照；次日复盘昨日时自动读取该缓存补全涨跌家数/成交额榜/大跌榜，无需重抓实时榜。
- 建议当天 15:00 收盘后运行，此时所有实时快照即为目标日收盘数据。

## 执行流程

1. 运行脚本。若沙箱无法访问国内财经接口（DNS/连接失败），以 `require_escalated` 重新运行；必要时为脚本设置 `proxies={}` 直连。
2. 读取脚本打印的核心指标（涨停/炸板/跌停、炸板率、最高板、昨涨停溢价与晋级率、强势板块、报告路径）。
3. 用报告数据按以下 6 步完成复盘并给用户结论：
   - 整体情绪：涨停/炸板/跌停 + 炸板率，昨日涨停溢价与晋级率，判断容错率。
   - 连板梯队：最高板、各板数家数、梯队是否断层、高标断板风险。
   - 板块结构：3 只以上涨停为强势板块，区分新爆发/延续扩散/退潮题材。
   - 资金流向：成交额前排收盘位置，龙虎榜净买/净卖，机构与游资席位风格。
   - 亏钱效应：炸板振幅与次数、昨日涨停大面、全市场大跌标的。
   - 风向标：报告第 6 节按硬条件筛选最多 10 只标的，展示板块状态、筛选指标与建仓建议。
4. 交付：在回复中给出 HTML 文件链接（绝对路径）并同步关键数字；避免仅丢文件不解读。

## 输出

脚本生成 `A股短线复盘_YYYY-MM-DD.html`，包含：盘面速览、整体情绪、连板梯队（含按板数列出的个股明细表）、板块结构、资金流向与龙虎榜、亏钱效应、风向标自选。报告为自包含单文件，可直接用浏览器打开。

已知口径：
- 龙虎榜净买/机构席位净买统一显示为“亿元”（接口原值为万元，脚本已换算）。
- 龙虎榜净卖按净卖金额从大到小展示；机构专用席位净卖展示 6 条，与手工版一致。
- 整体情绪“结论”与连板梯队“断层信号”按手工版口径生成：涨停家数收缩时提示“有肉但热度在退”，断层提示高标断板与最高板交接。
- 章节说明按手工版口径：涨停收缩时整体情绪显示“容错率中等偏上，赚钱效应在收缩”；梯队说明显示“N板独木撑高度，X-Y板断层”。
- 连板梯队评价：最高板标“总龙头”并附天数连板统计（如 21天11板）；次高板成员炸板≥3次时标“板质偏弱”并附炸板次数，其余按“N 只”展示。
- 连板梯队明细表：按板数列出全部涨停个股，含 板数/代码/名称/行业/连板统计/换手率/封单成交比(%)/最后封板时间；最后封板时间早于 09:45:00 的个股整行青色高亮并加粗时间；`封单成交比 = 交易结束前买一挂单总金额 / 当日成交总金额 × 100%`（封单金额、成交金额均为元），>100% 用红色高亮表示封单强度高、较难开板，值越低封单防守越弱、开板风险越大。
- 可转债（代码 11/12/13 开头）从龙虎榜与机构席位统计中过滤；“自然人/其他自然人/机构投资者”聚合席位不列入游资活跃席位，席位名称做简写处理。
- “昨日高标断板”取昨涨停池中连板数最高的断板股，而非涨幅榜首位。
- 板块状态按昨日与今日涨停行业计数生成：昨日 ≥3 且今日 ≥ 昨日为“延续扩散”；昨日 ≥3 且今日 < 昨日为“退潮”；今日强势板块但昨日 <3 为“新爆发”；昨日 ≥3 且今日归零按 `N→0` 展示为退潮。
- 风向标硬筛选条件：`封单成交比 > 300%`、`换手率 < 3%`、`首次封板时间 < 09:35:00`；按首次封板时间升序、封单成交比降序取前 10 只。表格包含 代码/名称/所属板块/板块状态/板块变化/封单比/换手率/首封时间/建仓建议，数据单元格单行显示，窄屏横向滚动。板块状态仅作展示列，不参与硬筛选；建仓建议按板块状态与封单强度生成情景化观察方案，不构成投资建议。

## 故障处理

- 涨停池为空：可能是非交易日或数据未更新，脚本会提示并退出；换 `--date` 重试。
- 东财 push2 排序接口偶发 `RemoteDisconnected`：脚本已内置新浪行情源兜底成交额/跌幅/涨跌家数，重试即可。
- 接口字段变化：字段细节与端点点位见 [references/api_notes.md](references/api_notes.md)，改脚本前先读该文件。

## 注意事项

- 务必在 A 股收盘后（15:00 之后）运行，否则为盘中数据。
- 公开接口有风控，脚本已内置东财 1s+ 串行限流；不要并发重复抓取。
- 报告含自动筛选的风向标与结论性文字，交付前由你核对数字并补充市场解读。
